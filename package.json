{
  "name": "myApp",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --driver-package practicalmeteor:mocha --port 5000",
    "features": "chimp --ddp=http://localhost:3000 --watch --cucumber --browser=phantomjs --path=features"
  },
  "dev-dependencies": {
    "chimp": "^0.35.0",
    "phantomjs-prebuilt": "^2.1.7",
    "faker": "^3.1.0"
  },
  "dependencies": {
    "material-ui": "^0.15.1",
    "meteor-node-stubs": "~0.2.0",
    "react": "^15.2.0",
    "react-dom": "^15.2.0",
    "react-router": "^2.5.2",
    "react-tap-event-plugin": "^1.0.0"
  }
}
