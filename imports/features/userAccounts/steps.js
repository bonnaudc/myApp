module.exports = function() {

    this.Given(/^the user "([^"]*)" has an account$/, function (arg1) {
           

            });

    this.When(/^he logs in$/, function () {
        // Write the automation code here
        pending();
    });

    this.Then(/^he should see "([^"]*)"$/, function (arg1) {
            // Write the automation code here
            pending();
            });

    this.Given(/^the user "([^"]*)" does not have an account$/, function (arg1) {
            // Write the automation code here
            pending();
            });

    this.Then(/^he sould see "([^"]*)"$/, function (arg1) {
            // Write the automation code here
            pending();
            });

    this.Given(/^the user does not have an account$/, function () {
        // Write the automation code here
        pending();
    });

    this.When(/^he signs up with "([^"]*)" name$/, function (arg1) {
            // Write the automation code here
            pending();
            });

    this.When(/^he signs up with "([^"]*)" as email$/, function (arg1) {
            // Write the automation code here
            pending();
            });
}

